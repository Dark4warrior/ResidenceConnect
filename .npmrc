# React Native + pnpm en monorepo : un node_modules à plat (comme npm)
# évite les copies dupliquées de react-native que Metro ne sait pas dédupliquer.
# Recommandé par la documentation Expo pour les monorepos pnpm.
node-linker=hoisted
